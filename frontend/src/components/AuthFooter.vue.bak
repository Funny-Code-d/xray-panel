<template>
  <p class="mt-6 text-center text-xs opacity-50">
    <RouterLink
      :to="{ name: 'about' }"
      class="hover:opacity-100 hover:underline transition"
    >
      О проекте
    </RouterLink>
  </p>
</template>